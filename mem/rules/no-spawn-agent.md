---
name: no-spawn-agent
description: Never use spawn_agent — burns project credits. Do all research/work directly.
type: constraint
---
- **NEVER** use `spawn_agent` / `acp_subagent--spawn_agent` under any circumstance.
- **Why:** burns project credits unnecessarily. User explicitly forbade it.
- **How to apply:** do all codebase research, file reads, web searches, and multi-step work directly in the main loop. Batch parallel tool calls instead of delegating to sub-agents.
